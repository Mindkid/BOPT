# BOPT

The Library folder has all the libraries needed to simulate MVRAM using pmdk.
To install it, run the script that it's inside the folder library.

The Source folder has the code of the program, it has an Makefile, to run the code run the makefile in the terminal.
